/**
 * Created by eugenia on 08.01.17.
 */
module.exports = (seed) => {
  return {
    firstName: `Fake Author ${seed}`,
    secondName: `Fake Author ${seed}`,
    lastName: `Fake Author ${seed}`,
    description: `fake description ${seed}`
  };
};